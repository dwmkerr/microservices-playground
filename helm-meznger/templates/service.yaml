apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-meznger.fullname" . }}
  labels:
    app: {{ include "helm-meznger.name" . }}
    chart: {{ include "helm-meznger.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "helm-meznger.name" . }}
    release: {{ .Release.Name }}
